# Team 027: Email and UI Polish

## Context
User requested updates to email output (duration units, date format) and UI font consistency.

## Objectives
1.  Email: `service_duration` -> "xx minutes".
2.  Email & UI: Date format -> `MM-DD-YYYY`.
3.  UI: Standardize fonts/sizes in `BookingSection.tsx`.
4.  Push to git.
